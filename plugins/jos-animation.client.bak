import "jos-animation/dist/jos.css";
import JOS from "jos-animation";

export default defineNuxtPlugin(() => {
    setTimeout(() => {
        // console.log(document.querySelectorAll(".jos"));
        JOS.init({
            animation: "grow",
            duration: 0.4,
            rootMargin: "10% 0% 40% 0%",
            debugMode: true,
        });
    }, 100);
});
